// slice() --> extracts a new String without modifying the existing String

let fullName='American Sniper'

let firstName;
let lastName;

firstName= console.log(fullName.slice(0,7)) //output: America
lastName=console.log(fullName.slice(9,14)) //output: Snipe